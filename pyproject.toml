[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dtm"
version = "0.1.0"
description = "Dense-to-MoE conversion utilities for Qwen-style models."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "Apache-2.0" }
dependencies = []

[project.optional-dependencies]
# Full inference / conversion runtime.
inference = [
    "torch>=2.0",
    "transformers>=4.40",
]
# Development: tests that exercise torch-based helpers need torch.
dev = [
    "pytest>=7",
    "torch>=2.0",
]
# All extras combined (convenience).
all = [
    "torch>=2.0",
    "transformers>=4.40",
    "pytest>=7",
]

[project.scripts]
dtm-convert = "dtm.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.setuptools.packages.find]
where = ["src"]

